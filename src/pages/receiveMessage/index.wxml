<view class="receiveContentPage">
    <text wx:if="{{receiveData.type == 'text'}}">{{ receiveData.content }}</text>
    <image wx:if="{{receiveData.type == 'image'}}" src="{{receiveData.content}}"></image>
</view>